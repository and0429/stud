import 'reset-css';

import '../css/index.less';

import './check';

import '@fortawesome/fontawesome-free/css/all.min.css';

function add(x, y) {
  return x + y;
}

// eslint-disable-next-line
console.log(add(3, 5));
